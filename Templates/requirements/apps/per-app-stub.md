# 🟡 App name

🟡 One-line description: what this app does and who its users are.

INSTRUCTION: The install runbook should gather this content via discussion. The runbook also renames this stub from `per-app-stub.md` to `<app-name>/_<app-name>.md` matching the app's actual name (with the `_` prefix). If the human defers on the description, leave the 🟡 markers and this INSTRUCTION line in place.

## Catalog

(No requirements yet.)
